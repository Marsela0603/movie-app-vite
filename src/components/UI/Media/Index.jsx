import styled from 'styled-components';

const Image = styled.img`
  max-width: 300px;
  height: auto;
  margin-top: 10px;
  margin-bottom: 10px;
  border-radius: 25px;
`;

const Media = {
  Image,
};

export default Media;
